name: guardian_dashboard
description: Guardian AI web dashboard (Flutter Web) — multi-branch overview, device health, reports. Placeholder; application scaffold pending approval.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  # Mandated stack (ENGINEERING.md) — added together with the first feature code:
  # flutter_riverpod, go_router, dio (via guardian_api_client)
  # guardian_core:
  #   path: ../packages/dart/guardian_core
  # guardian_api_client:
  #   path: ../packages/dart/guardian_api_client
  # guardian_ui:
  #   path: ../packages/dart/guardian_ui

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
