"""Vision infrastructure: detector implementations and overlay rendering.

Model runtime backends (ONNX Runtime, TensorRT) live in
``infrastructure/inference`` once they exist; this package holds
task-level detectors and presentation of their results.
"""
