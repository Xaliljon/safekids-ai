import 'package:flutter_test/flutter_test.dart';
import 'package:guardian_ui/guardian_ui.dart';

void main() {
  test('package declares a version', () {
    expect(guardianUiVersion, isNotEmpty);
  });
}
