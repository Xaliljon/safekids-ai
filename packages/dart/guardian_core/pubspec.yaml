name: guardian_core
description: Shared Guardian AI domain entities and value objects. Pure Dart — no Flutter, no I/O, no app dependencies.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies: {}

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
