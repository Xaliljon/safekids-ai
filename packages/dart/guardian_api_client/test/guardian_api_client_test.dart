import 'package:guardian_api_client/guardian_api_client.dart';
import 'package:test/test.dart';

void main() {
  test('package declares a version', () {
    expect(guardianApiClientVersion, isNotEmpty);
  });
}
