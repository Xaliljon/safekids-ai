# Copy to docker/.env and adjust. Never commit .env (see .gitignore).
# These defaults are for LOCAL DEVELOPMENT ONLY.

POSTGRES_DB=guardian
POSTGRES_USER=guardian
POSTGRES_PASSWORD=guardian-dev-only

RABBITMQ_USER=guardian
RABBITMQ_PASSWORD=guardian-dev-only
